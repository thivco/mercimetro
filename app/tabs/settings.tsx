import { Text, View } from "react-native";

export default function Tab() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <h1>Settings here</h1>
            {/*  Choose theme and other preferences */}
    </View>
  );
}
